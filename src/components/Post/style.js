import { makeStyles } from "@material-ui/core/styles";
export const useStyles = makeStyles((theme) => ({
  root: {
    marginTop: "70px",
    marginLeft: "auto",
    marginRight: "auto",
    maxWidth: "50%",
  },
}));
